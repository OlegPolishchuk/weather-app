import React from 'react';

import s from './MonthStatistics.module.scss';

import { ReturnComponentType } from 'types';

export const MonthStatistics = (): ReturnComponentType => {
  return <div className={s.month} />;
};
